## Installation

## Usage In Laravel

The `YouCan\FPay\Providers\LaravelProvide` contains the binding for the `Requestor` class which is needed to make request to the FPay API.

To make it work, you need to add `CLIENT_ID`, `MERCHANT_KEY` and `MERCHANT_CODE` to your environment config.
